version: 2

jobs:
  build:
    working_directory: ~/work

    docker:
      - image: circleci/openjdk:8

    steps:
      - checkout

      - restore_cache:
          key: lol-{{ checksum "build.gradle" }}-{{ checksum ".circleci/config.yml" }}

      - run: ./gradlew build

      - save_cache:
          paths:
            - ~/.gradle
          key: lol-{{ checksum "build.gradle" }}-{{ checksum ".circleci/config.yml" }}

      - store_test_results:
          path: build/reports
